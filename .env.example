# Environment Variables for MarTechNexus

# Database Configuration (Required for production)
# For local development, you can use a local PostgreSQL database
# For production on Render, this will be automatically provided
DATABASE_URL=postgresql://username:password@localhost:5432/martechnexus

# Node Environment
NODE_ENV=development

# Port (automatically set by Render in production)
PORT=5000

# Development-only variables
SEPARATE_CLIENT=true
